# Local development database for the SQLx compile-time query cache (ADR-002).
#
# Contains no credentials: SQLite is a file, and this path lives under the
# gitignored target/ directory. Created by `just bootstrap`.
#
# Developer-specific overrides belong in .env.local, which is gitignored.
DATABASE_URL=sqlite:target/grid-dev.db
